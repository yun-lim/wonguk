{
  "name": "wunguk",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 8787",
    "build": "vite build",
    "preview": "vite preview --host 127.0.0.1 --port 8787",
    "test": "vitest run"
  },
  "dependencies": {
    "manseryeok": "^2.0.0"
  },
  "devDependencies": {
    "vite": "^6.3.5",
    "vitest": "^3.2.4"
  }
}
